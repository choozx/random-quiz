# 이 파일을 복사해서 ".env.local" 로 저장한 뒤 값을 채우세요.
#   cp .env.example .env.local
# .env.local 은 깃에 올라가지 않습니다(비밀값 보호). 컴퓨터마다 따로 만들어야 합니다.

# 유튜브 Data API 키 (AIza... 로 시작)
VITE_YT_API_KEY=

# 재생할 유튜브 재생목록 — URL 전체 또는 list= 뒤의 ID
VITE_YT_PLAYLIST=
